<!--Objective: custom route making; just click on map to add markers and they will link up.-->
<% provide(:title, "Location") %>

<head>
  <%= javascript_include_tag "fixed_map" %>
  <!-- In config/initializers/assets.rb include line:
	Rails.application.config.assets.precompile += %w( <script_name>.js ) -->

  <script src="https://maps.googleapis.com/maps/api/js?&callback=initialize" async defer>
  </script>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <% render 'layouts/shim' %>


</head>

<body >
    <%= render 'layouts/header' %>
  <div class="body_map">
    <div class="container">
        <h3>The map</h3>
        <div id ="map">
        </div>
    </div>
  </div>
</body>

<%= debug(params) if Rails.env.development? %>


